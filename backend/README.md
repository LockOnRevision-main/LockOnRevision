# Backend Scaffold

Firebase is currently unavailable, so backend code is intentionally not implemented.

Planned backend responsibilities:

- Authenticated score update endpoint
- Admin-only cohort and moderation endpoints
- Leaderboard maintenance jobs if needed
- Firestore rule validation support
- Audit logs for XP and Energy changes

The active Firebase Cloud Functions folder is `functions/`.
